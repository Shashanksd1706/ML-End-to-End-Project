# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and Push to ACR

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Log in to Azure Container Registry
      run: |
        echo ${{ iWy+EGBODdGr8w4EFuKesjoty6BSuz2JYnIm/2fSBC+ACRAAg315 }} | docker login testdckershashank.azurecr.io --username ${{ testdckershashank }} --password-stdin
        
    - name: Build and Push Docker image
      run: |
        docker build -t testdckershashank.azurecr.io/performanceindicator:latest .
        docker push testdckershashank.azurecr.io/performanceindicator:latest
        echo "✅ Image successfully built and pushed to ACR!"


