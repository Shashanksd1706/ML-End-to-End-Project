# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ML Performance Indicator

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Log in to Azure Container Registry
      run: |
        echo ${{ iWy+EGBODdGr8w4EFuKesjoty6BSuz2JYnIm/2fSBC+ACRAAg315 }} | docker login testdckershashank.azurecr.io --username ${{ testdckershashank }} --password-stdin
        
    - name: Build Docker image
      run: |
        docker build -t testdckershashank.azurecr.io/performanceindicator:latest .
        docker build -t testdckershashank.azurecr.io/performanceindicator:${{ github.sha }} .
        
    - name: Push Docker image
      run: |
        docker push testdckershashank.azurecr.io/performanceindicator:latest
        docker push testdckershashank.azurecr.io/performanceindicator:${{ github.sha }}
        
    - name: Deploy to Azure Web App (if configured)
      run: |
        echo "Image pushed successfully!"
        echo "You can now manually deploy to Azure Web App or configure additional steps"

